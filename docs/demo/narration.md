# SQLTask 데모 영상 — 내레이션 스크립트

총 14개 씬 / 228.8초 (3.8분) · 1920x1080 30fps · TTS: macOS `say -v Yuna -r 178`

## 01. 타이틀  <small>(00:00.00 · 13.3s)</small>

유이엔진 식스의 새 비피엠엔 태스크, 에스큐엘 태스크를 소개합니다. 프로세스가 흘러가는 도중에, 설정된 데이터베이스에 접속해서 에스큐엘을 바로 실행하는 태스크입니다.

## 02. 배경 — 두 액티비티를 하나로  <small>(00:13.28 · 23.3s)</small>

예전 유이엔진 비피엠에는 에스큐엘 액티비티와 데이터베이스 매핑 액티비티, 두 개의 타입이 따로 있었습니다. 게다가 데이터베이스 매핑 액티비티는 내부에서 에스큐엘 액티비티를 익명 클래스로 다시 만들어 쓰는 구조였습니다. 이번에는 이 둘을 하나의 에스큐엘 태스크로 합치고, 설정 방식만 스트래티지로 갈라놓았습니다.

## 03. 구조  <small>(00:36.54 · 22.3s)</small>

구조는 이렇습니다. 에스큐엘 태스크가 커넥션 획득, 물음표 파라미터 바인딩, 타입 변환, 그리고 결과를 프로세스 변수로 되돌리는 공통 실행부를 전부 담당합니다. 스트래티지는 어떤 에스큐엘을 만들 것인가, 그것만 결정합니다. 접속할 데이터베이스는 커넥션 팩토리로 갈아 끼웁니다.

## 04. 모델링 ① SQL 직접 작성  <small>(00:58.89 · 19.0s)</small>

먼저 모델링 타임입니다. 비피엠엔에서는 서비스 태스크로 그리고, 유이엔진 프로퍼티스 제이슨에 언더바 타입으로 에스큐엘 태스크임을 선언합니다. 에스큐엘을 직접 쓰는 방식에서는, 물음표 자리에 들어갈 파라미터를 순서대로 프로세스 변수와 연결합니다.

## 05. 모델링 ② 데이터베이스 매핑  <small>(01:17.85 · 25.1s)</small>

같은 태스크에 스트래티지만 데이터베이스 매핑으로 바꾸면, 에스큐엘을 한 줄도 쓰지 않습니다. 테이블 점 컬럼 형태로 프로세스 변수를 매핑하고, 쿼리 모드만 고르면 됩니다. 키로 지정한 컬럼은 웨어 절이 되고, 나머지가 대상 컬럼이 됩니다. 셀렉트, 인서트, 업데이트, 딜리트 네 가지 모드에서 이렇게 에스큐엘이 만들어집니다.

## 06. 모델러 등록  <small>(01:42.92 · 16.4s)</small>

백오피스 모델러에도 에스큐엘 태스크를 등록했습니다. 팔레트에서 태스크 타입을 에스큐엘 태스크로 바꾸면 전용 속성 패널이 열리고, 연결 방식과 설정 방식, 에스큐엘과 컬럼 매핑을 화면에서 편집할 수 있습니다.

## 07. 실행 타임 인트로  <small>(01:59.34 · 11.3s)</small>

이제 실행 타임입니다. 에이치투 인메모리 데이터베이스를 띄우고, 실제로 돌려보겠습니다. 지금부터 보이는 출력은 전부 실제 실행 결과입니다.

## 08. 라이브 ① DirectSQL  <small>(02:10.60 · 8.5s)</small>

먼저 에스큐엘을 직접 쓰는 방식입니다. 인서트 문을 그대로 실행해서, 고객 한 건이 테이블에 들어갔습니다.

## 09. 라이브 ② 매핑 INSERT  <small>(02:19.07 · 11.8s)</small>

다음은 같은 태스크를, 컬럼 매핑만으로 설정한 경우입니다. 에스큐엘을 쓰지 않았는데 인서트 문이 생성되어 실행됐고, 두 번째 고객이 들어갔습니다.

## 10. 라이브 ③ 매핑 UPDATE  <small>(02:30.85 · 12.2s)</small>

쿼리 모드를 업데이트로 바꾸면, 키로 지정한 아이디 컬럼이 웨어 절로 빠지고 나머지 컬럼이 셋 절이 됩니다. 씨 마이너스 일 번 고객만 수정된 것을 볼 수 있습니다.

## 11. 라이브 ④ 매핑 SELECT  <small>(02:43.06 · 15.7s)</small>

셀렉트 모드에서는 조회 결과가 프로세스 변수로 돌아옵니다. 고객 이름과 여신 한도가 데이터베이스에서 읽혀서, 인스턴스 변수에 그대로 담겼습니다. 여러 행이 조회되면 멀티플 프로세스 변수가 됩니다.

## 12. 라이브 ⑤ 매핑 DELETE  <small>(02:58.79 · 13.4s)</small>

마지막으로 딜리트입니다. 키 컬럼만으로 웨어 절이 만들어져서, 해당 고객만 지워졌습니다. 하나의 태스크에서 스트래티지만 바꿔가며 네 가지 동작을 모두 처리했습니다.

## 13. 테스트 결과  <small>(03:12.19 · 27.0s)</small>

자동화 테스트로도 검증했습니다. 실행 타임 테스트 열두 건, 모델링 타임 테스트 여덟 건이 모두 통과합니다. 특히 모델링 타임 테스트에는, 비피엠엔 파일을 파싱해서 만든 정의를 그대로 실행해 보는 케이스가 들어 있습니다. 인서트 문에서 어퍼 함수로 대문자로 저장한 뒤, 데이터베이스 매핑 셀렉트가 그 대문자 값을 프로세스 변수로 되돌리는지까지 확인합니다.

## 14. 마무리  <small>(03:39.22 · 9.6s)</small>

추가된 파일과 문서입니다. 자세한 설정 방법은 독스 폴더의 에스큐엘 태스크 문서를 참고하시면 됩니다. 이상입니다.
