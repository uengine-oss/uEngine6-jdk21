# SQLTask 모델러 데모 — 내레이션 스크립트

총 11개 씬 / 276.4초 (4.6분) · 1920x1080 30fps

음성: **XTTS-v2**(coqui-tts) 제로샷 음성복제, 26초 참조. `atempo=1.22` 로 속도 보정.
각 문장은 Whisper large-v3-turbo 로 다시 받아써 원문과 CER 을 비교해 검증했다 (평균 0.021, 최악 0.082). 생성 스크립트: `/Users/uengine/tts`

> IndexTTS-2.5 는 한국어를 지원하지 않아(중·영·일·스·아) 사용하지 못했다. `/Users/uengine/tts/README.md` 참고.

04~07 은 실제 모델러 화면 녹화 (`playwright/sqltask-configure.mjs`, 원본 94초).

## 01. 타이틀  <small>(00:00.00 · 14.9s · CER 0.000)</small>

지난 데모는 제가 모델러를 잘못 짚었습니다. 이번에는 실제로 돌아가는 프론트엔드에서 에스큐엘 태스크를 직접 설정하고, 저장하고, 실행하는 것까지 전부 화면으로 보여드리겠습니다.

## 02. 무엇을 잘못 짚었나  <small>(00:14.87 · 21.3s · CER 0.000)</small>

먼저 정정입니다. 제가 처음에 컴포넌트를 넣은 백오피스는 오픈그래프 기반의 레거시 모델러였고, 지금 실제로 뜨는 화면은 별도 저장소에 있는 비피엠엔 제이에스 모델러였습니다. 게다가 이 모델러는 태스크 타입별 뷰가 아니라, 비피엠엔 엘리먼트 종류로 속성 패널을 고릅니다.

## 03. 어떻게 붙였나  <small>(00:36.17 · 31.3s · CER 0.025)</small>

그래서 접근을 바꿨습니다. 에스큐엘 태스크는 비피엠엔 상 서비스 태스크로 그리고, 언더바 타입으로만 구분하기 때문에, 서비스 태스크 패널에 구현 방식이라는 선택을 하나 넣었습니다. 레스트 호출을 고르면 예전 그대로고, 에스큐엘 실행을 고르면 에스큐엘 설정 화면이 열립니다. 그리고 컬럼 매핑은 유저 태스크에서 쓰던 폼 매퍼를 그대로 쓰고, 대상 테이블과 컬럼, 기본키는 데이터베이스에서 직접 읽어옵니다.

## 04. [화면] 정의 열고 편집 모드  <small>(01:07.48 · 12.2s · CER 0.018)</small>

실제 화면입니다. 프로세스 정의 맵에서 데모용 정의를 열면 비피엠엔 제이에스 모델러가 뜨고, 연필 버튼으로 잠금을 풀어 편집 모드로 들어갑니다.

## 05. [화면] 고객 등록 — SQL 직접 작성  <small>(01:19.65 · 19.5s · CER 0.011)</small>

첫 번째 태스크, 고객 등록입니다. 속성 패널 맨 위의 구현 방식을 에스큐엘 실행으로 바꾸면 에스큐엘 입력창과 파라미터 표가 나타납니다. 인서트 문을 쓰고, 물음표 자리에 들어갈 프로세스 변수를 순서대로 연결합니다.

## 06. [화면] 고객 조회 — 테이블은 DB 에서, 매핑은 폼 매퍼로  <small>(01:39.12 · 36.1s · CER 0.005)</small>

두 번째 태스크, 고객 조회는 설정 방식을 데이터베이스 매핑으로 바꿉니다. 대상 테이블은 데이터베이스에서 읽어온 목록에서 고르기만 하면 되고, 컬럼과 기본키는 자동으로 따라옵니다. 유이엔진 3 편집기의 테이블즈, 리프레시 버튼과 같은 자리입니다. 그다음 컬럼 매핑 편집을 누르면, 유저 태스크에서 쓰던 그 폼 매퍼가 열립니다. 왼쪽에는 프로세스 변수, 오른쪽에는 커스터머 테이블의 컬럼이 트리로 뜨고, 선을 긋는 것만으로 매핑 엘리먼트가 만들어집니다.

## 07. [화면] 기본키 → WHERE 절 → 저장  <small>(02:15.19 · 17.6s · CER 0.034)</small>

매퍼에서 저장하면 패널에 매핑 결과가 뜹니다. 기본키인 아이디는 키로 이미 체크되어 있고, 그래서 생성될 에스큐엘의 웨어 절로 빠집니다. 마지막으로 버전 업 다이얼로그에서 저장하면 정의 파일에 반영됩니다.

## 08. 저장된 정의  <small>(02:32.75 · 26.3s · CER 0.053)</small>

저장된 정의 파일입니다. 서비스 태스크의 유이엔진 프로퍼티스에 언더바 타입이 에스큐엘 태스크로 들어갔고, 커넥션 팩토리와 에스큐엘, 파라미터가 그대로 저장됐습니다. 두 번째 태스크에는 매퍼가 만든 매핑 엘리먼트가 그대로 들어가 있습니다. 폼 매핑과 완전히 같은 구조라서, 키 표시만 더 붙습니다.

## 09. 실행 (PostgreSQL)  <small>(02:59.08 · 25.2s · CER 0.008)</small>

이제 매퍼로 만든 이 정의로 인스턴스를 시작합니다. 여신 한도를 일부러 천오백만이라는 콤마 붙은 문자열로 넣었습니다. 포스트그레스 커스터머 테이블을 보면 고객 등록 태스크가 넣은 한 행이 있고, 콤마가 빠진 숫자로 들어갔습니다. 그리고 고객 조회 태스크가 그 값을 다시 프로세스 변수로 돌려놨습니다.

## 10. 고친 것들  <small>(03:24.30 · 58.3s · CER 0.082)</small>

먼저, 컬럼 매핑을 제가 만든 표로 하고 있었는데, 유저 태스크의 폼 매퍼를 쓰라는 지적을 받고 그대로 교체했습니다. 같은 매핑 컨텍스트 구조라 서버는 손댈 것이 없었습니다. 이어서 유이엔진 3 데모 영상을 보니, 컬럼을 손으로 적는 게 아니라 데이터베이스에서 읽어오고 있었습니다. 그래서 메타데이터 조회 엔드포인트를 추가해, 테이블과 컬럼, 기본키를 자동으로 채우도록 했습니다. 그리고 실제로 돌려보니 유닛 테스트만으로는 못 잡을 문제가 더 나왔습니다. 파라미터에는 변수 이름만 있고 타입은 정의에 있어서, 전부 문자열로 바인딩되고 있었습니다. 에이치투는 받아주지만 포스트그레스는 거절합니다. 선언 타입을 찾아 쓰도록 고치고 회귀 테스트를 추가했습니다. 그리고 패널이 다시 그려지면 설정이 날아가던 문제와, 정의 잠금이 남아 편집이 막히던 문제도 함께 정리했습니다.

## 11. 정리  <small>(04:22.62 · 13.8s · CER 0.000)</small>

정리하면, 백엔드 에스큐엘 태스크는 그대로고, 프론트엔드는 실제 모델러 쪽으로 옮겨 붙였습니다. 모델링부터 저장, 실행까지 한 번에 확인했습니다. 이상입니다.
